diff --git a/dist/index.js b/dist/index.js
index b26359bbb96d9ad476d499bf32c39b77a7b5312f..3928079a522da074771cd86c7537acfe0e095edb 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -79954,7 +79954,7 @@ Use \`background_output(task_id="${task.id}")\` to retrieve this result when rea
         await this.client.session.promptAsync({
           path: { id: task.parentSessionID },
           body: {
-            noReply: !allComplete,
+            noReply: true,
             ...agent !== undefined ? { agent } : {},
             ...model !== undefined ? { model } : {},
             ...resolvedTools ? { tools: resolvedTools } : {},
@@ -79964,7 +79964,7 @@ Use \`background_output(task_id="${task.id}")\` to retrieve this result when rea
         log("[background-agent] Sent notification to parent session:", {
           taskId: task.id,
           allComplete,
-          noReply: !allComplete
+          noReply: true
         });
       } catch (error92) {
         if (isAbortedSessionError(error92)) {
